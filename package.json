{
    "scripts": {
        "build": "cd client && npm run build",
        "install-fullstack": "concurrently \"cd back-qwerty-shop && npm install\" \"cd front-qwerty-shop && npm install\"",
        "start": "concurrently \"cd back-qwerty-shop && npm start\" \"cd front-qwerty-shop && npm start\""
    },
    "devDependencies": {
        "concurrently": "^6.2.0"
    },
    "dependencies": {
        "apollo3-cache-persist": "^0.10.0",
        "graphql-scalar-objectid": "^0.1.2"
    }
}
